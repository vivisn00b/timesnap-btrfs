#!/bin/bash
# overlay_snap_rw.install
# mkinitcpio install file for snapshot RW remounting

build() {
    add_module btrfs
    add_binary btrfs
    add_binary blkid
    add_runscript
}

help() {
    cat <<HELPEOF
Boot into a Btrfs snapshot and remount it read-write for permanent changes.
HELPEOF
}
